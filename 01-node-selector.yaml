apiVersion: v1
kind: Pod
metadata:
  name: nginx
  labels:
    project: roboshop
    namespace: roboshop
    tier: database
spec:
  - name: nginx
    image: nginx
    ports:
      - name: nginx-port
        containerPort: 80
    # nodeSelector:
    #   project: roboshop