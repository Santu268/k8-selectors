apiVersion: v1
kind: Pod
metadata:
  name: nginx
  labels:
    project: roboshop
    tier: database
spec:
  containers:
  - name: nginx
    image: nginx
    ports:
      - name: nginx-port
        containerPort: 80
  nodeSelector:
      project: roboshop